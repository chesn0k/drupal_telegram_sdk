<?php

namespace Drupal\{{ machine_name }}\Plugin\TelegramCommand;

use Drupal\drupal_telegram_sdk\Plugin\TelegramCommand\TelegramCommandPluginBase;

/**
 * Provides a {{ plugin_name }} telegram command.

 * @TelegramCommand(
 *   id = "{{ plugin_id }}",
 *   name = "{{ plugin_name }}",
 *   description = @Translation("{{ description }}"),
{% if bots_id %}
 *   bots_id = {
  {% for bot_id in bots_id %}
 *     "{{ bot_id }}",
  {% endfor %}
 *   },
{% endif %}
{% if aliases %}
 *   aliases = {
  {% for alias in aliases %}
 *     "{{ alias }}",
  {% endfor %}
 *   }
{% endif %}
{% if pattern %}
 *   pattern "{{ pattern }}"
{% endif %}
 * )
 */
class {{ class }} extends TelegramCommandPluginBase {

   /**
   * {@inheritDoc}
   */
   public function handle() {
      $this->replyWithMessage(['text' => 'Hello world!']);
   }

}
